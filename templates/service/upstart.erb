start on startup and started network
stop on shutdown

respawn

exec su -s /bin/sh -c 'exec "$0" "$@"' <%= scope['::oauth2_proxy::user'] %> -- /opt/oauth2_proxy/bin/oauth2_proxy --config=/etc/oauth2_proxy/oauth2_proxy.conf
